server:
   port: 8085

jwt:
 secret: ${JWT_SECRET:RI9t7hmW0/FXdfES6yID6p8v56okBMr5XH/CYh6AcjU=}
 
spring:
   cloud:
      gateway:
         routes:
         - id: bms-authuser-service
           uri: lb://BMS-AUTHUSER-SERVICE
           predicates:
           - Path=/bms/auth/**
           filters:
           - JwtAuthenticationFilter

        - id: account-service
          uri: lb://ACCOUNT-MANAGEMENT-SERVICE
          predicates:
          - Path=/accounts/**
          filters:
          - JwtAuthenticationFilter 


eureka:
   client:
      register-with-eureka: true
      fetch-registry: true
